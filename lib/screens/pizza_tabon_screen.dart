import 'package:flutter/material.dart';

class PizzaTabonScreen extends StatelessWidget {
  const PizzaTabonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pizza Tabon"),
      ),
    );
  }
}
